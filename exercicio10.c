#include <stdio.h>

/*
    Ler duas strings A e B e mostrar quantas vezes a string A ocorre dentro da string B.
*/

int main ()
{
    char strA[100], strB[100];
    int cont = 0, i , j;

    scanf(" %[^\n]", strA);
    scanf(" %[^\n]", strB);

    i = strlen(strA);
    j = strlen(strB);

    //verificar quantas vezes a string A ocorre dentro de B

    


    return 0;
}